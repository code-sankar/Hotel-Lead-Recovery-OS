export function MobileNavNote() {
  return (
    <p className="border-b border-ink-200 bg-warm-50 px-4 py-2 text-[12px] text-warm-700 md:hidden">
      This dashboard is built for a laptop. Everything works on a phone, but the inbox is easier on a
      bigger screen.
    </p>
  );
}
